<div class="container">
    <div class="row">
        <div class="col-lg-12" style="font-size: 30px;font: bold">
            <!-- <% const current = parseInt(req.query.page) || 1; %>

            <nav aria-label="Page navigation example">
                <ul class="pagination">
                    <li class="page-item">
                        <a class="page-link" href="/user/<%=req.session.user_id%>/register/?page=<%= Math.max(current-1, 1) %>">Previous</a>
                    </li>

                    <% for (i = 1; i <= count; i++) { %>
                    <li class="page-item">
                        <a class="page-link" href="/user/<%=req.session.user_id%>/?page=<%= i %>">
                            <%= i %></a>
                    </li>
                    <% } %>

                    <li class="page-item">
                        <a class="page-link" href="/user/<%=req.session.user_id%>/?page=<%= Math.min(current+1, count) %>">Next</a>
                    </li>
                </ul>
            </nav> -->
            <b>Registered events</b>
        </div>
        <div class="row">


            <% model.forEach( function(model) { %>
            <div class="card col-lg-4">
                <div><a href="/event/view/<%= model.id %>"><img src="<%=model.imageurl%>" height=200 width=350></a><br><br>
                    <a href='/event/view/<%= model.id %>' style="font-size:30px;margin: 20px">
                        <%= model.eventname %></a>
                    <div class="card" style="margin: 20px">
                        <table>
                            <tr>
                                <td>
                                    Date:
                                    <%= model.eventdate %>
                                </td>
                            </tr>
                            <tr>
                                <td>
                                    Time:
                                    <%= model.time %>
                                </td>
                            </tr>
                            <tr>
                                <td>
                                    Venue:
                                    <%= model.venue %>
                                </td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>

            <% }); %>
        </div>
    </div>
</div>